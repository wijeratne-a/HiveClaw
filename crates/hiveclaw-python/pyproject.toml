[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "hiveclaw_python"
requires-python = ">=3.11"
dynamic = ["version"]

[tool.maturin]
module-name = "hiveclaw_python._core"
python-source = "python"
manifest-path = "Cargo.toml"
